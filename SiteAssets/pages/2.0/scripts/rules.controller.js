// RULES CONTROLLER
// MANAGE RULES CENTRALLY
// **********************
// RULES BASED ON HAZARD TYPES
// CONDITIONS

writeHazard={
    item:function(h){
        var rowtitle=checkHazard.category(h);
        var rowFooter=checkHazard.credentials(h);
        var 
    }
};